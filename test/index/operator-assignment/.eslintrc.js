module.exports = {
    rules: {
        /**
         * @memberof stylistic-issues
         * @description @fixable 必须使用 x = x + y 而不是 x += y
         * @off 没必要限制
         */
        'operator-assignment': 'off'
    }
};
