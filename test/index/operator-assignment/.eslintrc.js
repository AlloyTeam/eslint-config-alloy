module.exports = {
    rules: {
        /**
         * @category stylistic-issues
         * @description 必须使用 x = x + y 而不是 x += y
         * @reason 没必要限制
         * @fixable
         */
        'operator-assignment': 'off'
    }
};
