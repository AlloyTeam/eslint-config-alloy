module.exports = {
    rules: {
        /**
         * @memberof best-practices
         * @description @fixable if 后面必须要有 {，除非是单行 if
         */
        curly: ['error', 'multi-line', 'consistent']
    }
};
