module.exports = {
    rules: {
        /**
         * @category best-practices
         * @description if 后面必须要有 {，除非是单行 if
         * @fixable
         */
        curly: ['error', 'multi-line', 'consistent']
    }
};
