module.exports = {
    rules: {
        /**
         * @memberof stylistic-issues
         * @description @fixable if, function 等的大括号之前必须要有空格，比如 if (a) {
         */
        'space-before-blocks': ['error', 'always']
    }
};
