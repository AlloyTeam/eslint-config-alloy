module.exports = {
    rules: {
        /**
         * @category ecmascript-6
         * @description import 必须按规则排序
         * @reason 没必要强制要求
         * @fixable
         */
        'sort-imports': 'off'
    }
};
