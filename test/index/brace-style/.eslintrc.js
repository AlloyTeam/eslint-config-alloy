module.exports = {
    rules: {
        /**
         * @memberof stylistic-issues
         * @description @fixable if 与 else 的大括号风格必须一致
         * @off else 代码块可能前面需要有一行注释
         */
        'brace-style': 'off'
    }
};
