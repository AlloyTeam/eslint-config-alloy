module.exports = {
    rules: {
        /**
         * @category stylistic-issues
         * @description if 与 else 的大括号风格必须一致
         * @reason else 代码块可能前面需要有一行注释
         * @fixable
         */
        'brace-style': 'off'
    }
};
