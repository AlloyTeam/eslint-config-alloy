module.exports = {
    rules: {
        /**
         * @memberof stylistic-issues
         * @description @fixable 操作符左右必须有空格，比如 let sum = 1 + 2;
         */
        'space-infix-ops': 'error'
    }
};
