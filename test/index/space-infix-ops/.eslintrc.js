module.exports = {
    rules: {
        /**
         * @category stylistic-issues
         * @description 操作符左右必须有空格，比如 let sum = 1 + 2;
         * @fixable
         */
        'space-infix-ops': 'error'
    }
};
