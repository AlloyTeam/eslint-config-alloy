module.exports = {
    rules: {
        /**
         * 操作符左右必须有空格，比如 let sum = 1 + 2;
         * @category Stylistic Issues
         * @fixable
         */
        'space-infix-ops': 'error'
    }
};
