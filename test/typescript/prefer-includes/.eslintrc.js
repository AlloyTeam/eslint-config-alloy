module.exports = {
    rules: {
        /**
         * 使用 includes 而不是 indexOf
         * @category TypeScript
         * @fixable
         * @requires-types-information
         */
        '@typescript-eslint/prefer-includes': 'off'
    }
};
