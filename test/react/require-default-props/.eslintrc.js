module.exports = {
    rules: {
        /**
         * @memberof
         * @description 非 required 的 prop 必须有 defaultProps
         * @off 不强制要求写 propTypes
         */
        'react/require-default-props': 'off'
    }
};
